{
  "name": "spotify-playlist-manager",
  "version": "1.0.0",
  "description": "Spotify Playlist Manager — Authorization Code with PKCE",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^8.2.2"
  }
}
